---
title: "Reference"
linkTitle: "Reference"
description: "The complete doc surface: every command and flag, the Go options and PRAGMA catalogue, the migration guide, the stability guarantees, the benchmarks, and the release history."
weight: 30
featured: true
---

The exhaustive reference.
The [CLI](/reference/cli/) page lists every `doc` command, flag, and shell dot-command.
The [configuration](/reference/configuration/) page covers the Go `Open` options, the PRAGMA catalogue, and the on-disk file conventions.
The [migration guide](/reference/migration-from-the-mongodb-go-driver/) maps the MongoDB Go driver onto doc.
The [architecture](/reference/architecture/) page maps the engine from the page on disk up to the wire server.
The [stability](/reference/stability/) page is honest about what is settled and what may still move before 1.0.
The [benchmarks](/reference/benchmarks/) page publishes measured numbers with the full setup.
The [release notes](/reference/release-notes/) summarise each version.
